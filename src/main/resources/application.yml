server:
  port: 8888

spring:
  application:
    name: configserver
  profiles:
    active: git
  cloud:
    config:
      server:
        git:
          uri: https://github.com/8bit-com/prop/ms-config-properties
          search-paths:
            - "/{application}/{profile}"
